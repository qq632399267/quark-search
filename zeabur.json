# Zeabur 部署配置
# 后端服务
services:
  - name: backend
    type: web
    path: backend
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8080

  - name: frontend
    type: static
    path: frontend
    buildCommand: npm install && npm run build
    outputDir: dist
